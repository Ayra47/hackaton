import s from './SettingsForm.module.scss';

export default function SettingsForm() {
    return (
        <div className={s.section__settings}>
            <div className={`${s.settings} w-container`}>
                <h1>settings page</h1>
            </div>
        </div>
    )
}